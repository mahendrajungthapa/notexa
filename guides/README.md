# Notexa Setup Guides

Use these guides in order for a local development setup:

- `backend/README.md` for the Laravel API
- `frontend/README.md` for the Next.js web app
- `app/README.md` for the Flutter app

Keep real credentials in local `.env` files only. The committed `.env.example` files are templates.
